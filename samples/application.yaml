apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: sample-app
spec:
  image: nginx
  port: 80
  resources:
    limits:
      memory: 100M
    requests:
      cpu: 5m
      memory: 50M
