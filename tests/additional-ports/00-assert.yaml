apiVersion: v1
kind: ServiceAccount
metadata:
  name: additional-ports
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: additional-ports
spec:
  selector:
    matchLabels:
      app: additional-ports
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
      labels:
        app: additional-ports
    spec:
      containers:
        - name: additional-ports
          image: image
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: main
              protocol: TCP
            - containerPort: 8181
              name: metrics
              protocol: TCP
            - containerPort: 8282
              name: some-udp-port
              protocol: UDP
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 150
            runAsUser: 150
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /tmp
              name: tmp
      imagePullSecrets:
        - name: github-auth
      securityContext:
        fsGroup: 150
        supplementalGroups:
          - 150
      serviceAccountName: additional-ports
      volumes:
        - emptyDir: {}
          name: tmp
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: additional-ports
spec:
  minReplicas: 1
  maxReplicas: 1
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: additional-ports
---
apiVersion: v1
kind: Service
metadata:
  name: additional-ports
spec:
  selector:
    app: additional-ports
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
      appProtocol: http
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: additional-ports
spec:
  podSelector:
    matchLabels:
      app: additional-ports
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: additional-ports
spec:
  selector:
    matchLabels:
      app: additional-ports
  mtls:
    mode: STRICT
