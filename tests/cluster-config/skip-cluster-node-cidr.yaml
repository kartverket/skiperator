apiVersion: v1
kind: ConfigMap
metadata:
  name: "skip-cluster-node-cidr"
  namespace: "skiperator-system"
data:
  config.yml: |
    clusters: 
      - name: "test-cluster-1"
        controlPlaneCIDRs:
          - 10.40.10.0/25
        workerNodeCIDRs:
          - 10.40.10.0/24

        
