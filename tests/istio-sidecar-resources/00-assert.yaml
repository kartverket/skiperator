apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio-sidecar-resource
spec:
  template:
    metadata:
      annotations:
        # Should use fallback 99m due to not running in a named env
        sidecar.istio.io/proxyCPU: "99m"
