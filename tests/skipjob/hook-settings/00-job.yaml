apiVersion: skiperator.kartverket.no/v1alpha1
kind: SKIPJob
metadata:
  name: presync-job
spec:
  container:
    image: image
  job:
    hookSettings:
      syncPhase: PreSync
---
apiVersion: skiperator.kartverket.no/v1alpha1
kind: SKIPJob
metadata:
  name: postsync-job
spec:
  container:
    image: image
  job:
    hookSettings:
      syncPhase: PostSync