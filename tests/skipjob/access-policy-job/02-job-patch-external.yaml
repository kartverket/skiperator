apiVersion: skiperator.kartverket.no/v1alpha1
kind: SKIPJob
metadata:
  name: access-policy-job
spec:
  container:
    image: "perl:5.34.0"
    command:
      - "perl"
      - "-Mbignum=bpi"
      - "-wle"
      - "print bpi(2000)"
    accessPolicy:
      outbound:
        rules:
          - application: minimal-application
        external:
          - host: example.com
            ports:
              - name: https
                port: 443
                protocol: HTTPS
          - host: foo.com
