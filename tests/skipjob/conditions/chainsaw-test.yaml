apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: conditions
spec:
  skip: false
  concurrent: true
  skipDelete: false
  steps:
   - try:
        - apply:
            file: skipjob.yaml
        - assert:
            file: skipjob-assert.yaml

#TODO this test must be rewritten, it wasn't working before refactor either.