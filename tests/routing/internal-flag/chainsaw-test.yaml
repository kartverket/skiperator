apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: routes-internal-flag
spec:
  skip: false
  concurrent: false
  skipDelete: false
  namespace: chainsaw-routing-internal-flag
  steps:
    - try:
        - apply:
            file: application.yaml
        - apply:
            file: routing-internal-flag.yaml
        - assert:
            file: routing-internal-flag-assert.yaml