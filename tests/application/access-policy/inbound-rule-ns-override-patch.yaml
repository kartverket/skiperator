apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: namespace-tests
spec:
  image: scratch
  port: 8080
  accessPolicy:
    inbound:
      rules:
        - application: test-ext
          namespacesByLabel:
            team: skiptest
            app: testapp
          namespace: nytt-ns