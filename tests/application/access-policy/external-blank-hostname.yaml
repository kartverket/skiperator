apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: empty-hostname
spec:
  image: image
  port: 8080
  accessPolicy:
    outbound:
      external:
        - host: maskinporten.no
        - host: ''
        - host: idporten.no
