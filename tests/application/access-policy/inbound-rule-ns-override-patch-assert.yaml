apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: namespace-tests
spec:
  podSelector:
    matchLabels:
      app: namespace-tests
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: nytt-ns
          podSelector:
            matchLabels:
              app: test-ext
      ports:
        - port: 8080
          protocol: TCP