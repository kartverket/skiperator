apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: ambient-app
  namespace: ambient-application
spec:
  image: image
  port: 8080
  ingresses:
    - ambient.example.com
  accessPolicy:
    inbound:
      rules:
        - application: caller
