apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: app-missing-internal-flag-ingress-df26180a7fa049ff
spec:
  selector:
    app: istio-ingress-internal
  servers:
    - hosts:
        - appmissinginternalflag.atkv3-dev.kartverket-intern.cloud
      port:
        name: http
        number: 80
        protocol: HTTP
    - hosts:
        - appmissinginternalflag.atkv3-dev.kartverket-intern.cloud
      port:
        name: https
        number: 443
        protocol: HTTPS
      tls:
        credentialName: chainsaw-routing-internal-flag-app-missing-internal-flag-ingress-df26180a7fa049ff
        mode: SIMPLE

---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: app-missing-internal-flag-ingress
spec:
  exportTo:
    - .
    - istio-system
    - istio-gateways
  gateways:
    - app-missing-internal-flag-ingress-df26180a7fa049ff
  hosts:
    - appmissinginternalflag.atkv3-dev.kartverket-intern.cloud
  http:
    - match:
        - port: 80
          withoutHeaders:
            :path:
              prefix: /.well-known/acme-challenge/
      name: redirect-to-https
      redirect:
        redirectCode: 308
        scheme: https
    - name: default-app-route
      route:
        - destination:
            host: app-missing-internal-flag
            port:
              number: 8000
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: app-missing-internal-flag
spec:
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-gateways
          podSelector:
            matchLabels:
              app: istio-ingress-internal
      ports:
        - port: 8000
          protocol: TCP
  podSelector:
    matchLabels:
      app: app-missing-internal-flag
  policyTypes:
    - Ingress
