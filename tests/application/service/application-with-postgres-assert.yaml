apiVersion: v1
kind: Service
metadata:
  name: service-props-pg
spec:
  selector:
    app: service-props-pg
  ports:
    - name: http
      port: 5432
      targetPort: 5432
      protocol: TCP
      appProtocol: tcp
