apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: stateful-invalid
spec:
  skip: false
  concurrent: true
  skipDelete: false
  steps:
    - try:
        - apply:
            file: application.yaml
        - assert:
            file: invalid-event-assert.yaml
