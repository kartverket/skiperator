apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: migration-app-custom-tls
  namespace: gateway-api-application-migration
spec:
  dnsNames:
    - migration-app-custom.example.com
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: migration-app-custom-tls
