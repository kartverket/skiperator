apiVersion: apps/v1
kind: Deployment
metadata:
  name: application
spec:
  selector:
    matchLabels:
      app: application
  template:
    spec:
      containers:
        - name: application
          volumeMounts:
            - mountPath: /tmp
              name: tmp
            - mountPath: /var/run/secrets/skip/maskinporten
              name: "maskinporten-application-07ab8a18"
      volumes:
        - emptyDir: {}
          name: tmp
        - name: "maskinporten-application-07ab8a18"
          secret:
            defaultMode: 420
            secretName: "maskinporten-application-07ab8a18"