apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: resources-modification
spec:
  skip: false
  concurrent: true
  skipDelete: false
  steps:
    - try:
        - create:
            file: applications.yaml
        - assert:
            file: applications-assert.yaml
    - try:
        - apply:
            file: patch-application.yaml
        - assert:
            file: patch-application-assert.yaml

