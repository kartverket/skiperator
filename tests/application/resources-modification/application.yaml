apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: resources-modification
spec:
  image: image
  port: 8080
  resources:
    limits:
      memory: 10Mi
    requests:
      cpu: 100m
      memory: 5Mi
