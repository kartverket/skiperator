apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: resources-modification
spec:
  image: image
  port: 8080
  resources:
    limits:
      memory: 20Mi
    requests:
      cpu: 200m
      memory: 10Mi
