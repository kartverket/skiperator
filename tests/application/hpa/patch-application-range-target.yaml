apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: test-deployment-hpa
spec:
  image: scratch
  port: 8080
  replicas:
    min: 3
    max: 6
    targetCpuUtilization: 60
