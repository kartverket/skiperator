apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test-priority
spec:
  podSelector:
    matchLabels:
      app: test-priority
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: nytt-ns
          podSelector:
            matchLabels:
              app: test-ext
      ports:
        - port: 8080
          protocol: TCP