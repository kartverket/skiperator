apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: authorization-settings-allow-list-allow
spec:
  rules:
    - from:
        - source:
            namespaces:
              - istio-gateways
      to:
        - operation:
            paths:
              - /actuator/info
        - operation:
            paths:
              - /actuator/shutdown
  selector:
    matchLabels:
      app: authorization-settings-allow-list
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: authorization-settings-allow-list-deny
spec:
  action: DENY
  rules:
    - from:
        - source:
            namespaces:
              - istio-gateways
      to:
        - operation:
            paths:
              - /actuator*
  selector:
    matchLabels:
      app: authorization-settings-allow-list
# This assertion does not work in the emulated control plane of Kuttl. See https://github.com/kudobuilder/kuttl/issues/471
# for progress regarding this issue
# TODO Add this back when issue fixed
#---
#apiVersion: security.istio.io/v1beta1
#kind: AuthorizationPolicy
#metadata:
#  name: authorization-settings-allow-all-deny
#spec:
#  action: DENY
#  rules:
#    - from:
#        - source:
#            namespaces:
#              - istio-gateways
#      to:
#        - operation:
#            paths:
#              - /actuator*
#  selector:
#    matchLabels:
#      app: authorization-settings-allow-all