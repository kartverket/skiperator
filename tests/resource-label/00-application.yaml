apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: test
spec:
  image: image
  port: 8080
  resourceLabels:
    - resourceKind:
        group: apps
        kind: Deployment
      labels:
        deploymentLabel: test
    - resourceKind:
        group: ""
        # also tests case insensitivity
        kind: service
      labels:
        serviceLabel: test
