apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: test
spec:
  image: image
  port: 8080
  resourceLabels:
    Deployment:
      labels:
        deploymentLabel: test
    Service:
      labels:
        serviceLabel: test
