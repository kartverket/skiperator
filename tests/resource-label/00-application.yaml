apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: test
spec:
  image: image
  port: 8080
  resourceLabels:
    - resourceKind:
        group: apps
        kind: Deployment
      labels:
        deploymentLabel: test
    - resourceKind:
        group: ""
        kind: Service
      labels:
        serviceLabel: test
