apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Configuration
metadata:
  name: configuration
spec:
  parallel: 10
  testDirs:
  - tests/application
  - tests/namespace
  - tests/skipjob
  timeouts:
    delete: 45s

