apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  namespace: istio-gateways
  name: fake-cert
spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: test-finalizer-ingress-56cd7aa901014e78
  dnsNames:
    - example.com
