apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
spec:
  template:
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: config-map
        - secretRef:
            name: secret