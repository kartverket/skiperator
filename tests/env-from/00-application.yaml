apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: test
spec:
  image: image
  port: 8080
  envFrom:
    - configMap: config-map
    - secret: secret
  resources:
    limits:
      memory: 100M
    requests:
      cpu: 5m
      memory: 50M
