apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: test
spec:
  image: image
  port: 8080
  ingresses:
    - example.com
  resources:
    limits:
      memory: 100M
    requests:
      cpu: 5m
      memory: 50M
